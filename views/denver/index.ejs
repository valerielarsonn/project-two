<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
        <link rel="stylesheet" href="path/to/font-awesome/css/font-awesome.min.css">
        <title></title>
    </head>
    <body>
        <nav>
            <a href="/">HOME PAGE</a>
            <a href="/denver/new">NEW DENVER LOCATION</a>
        </nav>
        <h1>Denver Spots</h1>
        <ul>
            <% for(let i = 0; i < denvers.length; i++){ %>
                <li>
                    NAME: <a href="/denver/<%=denvers[i].id; %>"><%=denvers[i].name; %></a> 
                    ADDRESS: <%=denvers[i].address; %>
                    HOURS: <%=denvers[i].hours; %> 
                    DAILY RATE: <%=denvers[i].daily; %> 
                    <% if(denvers[i].monthly === true){ %>
                        This location has a monthly rate option.
                    <% } else { %>
                        This location does not have a monthly rate option.
                    <% } %>
                    <img src="<%= denvers[i].imageUrl %>">
                    <form action="/denver/<%=denvers[i].id; %>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form>
                    <a href="/denver/<%=denvers[i].id; %>/edit">Edit</a>
                </li>
            <% } %>
        </ul>
    </body>
</html>