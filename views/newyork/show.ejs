<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
        <link rel="stylesheet" href="path/to/font-awesome/css/font-awesome.min.css">
        <title></title>
    </head>
    <body>
        <h1> New York show page</h1>
        NAME: <%=newyork.name; %>
        ADDRESS: <%=newyork.address; %>
        HOURS: <%=newyork.hours; %> 
        DAILY RATE: <%=newyork.daily; %> 
        <% if(newyork.monthly === true){ %>
            This location has a monthly rate option.
        <% } else { %>
            This location does not have a monthly rate option.
        <% } %>
        <img src="<%= newyork.imageUrl %>">
        <nav>
            <a href="/newyork">Back to New York Locations</a>
            <a href="/newyork/:id/reviews/new">Leave a Review</a>
            <a href="/newyork/:id/reviews">See Reviews</a>
        </nav>
    </body>
</html>